spring:
  datasource:
    url: jdbc:h2:mem:fitness_coaching;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

auth:
  jwt:
    issuer: fitness-coaching-backend-test
    secret: test-only-jwt-secret-1234567890-abcdefghijklmnopqrstuvwxyz
