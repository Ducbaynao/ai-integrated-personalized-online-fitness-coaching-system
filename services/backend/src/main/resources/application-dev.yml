spring:
  config:
    activate:
      on-profile: dev
  jpa:
    show-sql: true

logging:
  level:
    com.fitnesscoaching.platform: DEBUG

auth:
  jwt:
    secret: dev-only-jwt-secret-change-before-production-1234567890
